1.Run GroceryStoreTest.exe
System will first do the automatical test and then user can input any other case to interact with it.
The input format is <product number> <product code>

2.Run GroceryStoreSetup.msi or GroceryStoreSetup.exe to install the program. The program will only response to user's input, without the automatical test cases.
The input format is <product number> <product code>